#Rosemary Davy
#Feberuary 18, 2021


import random           #import random module
x = (random.randrange(1, 101, 2))      #assign x to a random integer between 1 & 100, and count by 2s, so only odd integers are returned
print(x) #print x
  
